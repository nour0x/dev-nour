# خطة Dev Nour الشاملة — من الألف إلى الياء

> مستند تشغيل وتطوير دقيق لمشروع **Dev Nour / نور محمد**  
> النطاق الحي: `https://dev.mudiridigi.com` · المستودع: `https://github.com/nour0x/dev-nour` · الفرع: `main`  
> آخر مزامنة للكود المرجعي: بعد تنفيذ الخطة (backup + seed-skills + CTA + meta + AEO)  
> حالة التنفيذ: **الكود منفَّذ ومرفوع** · **السيرفر Hostinger يحتاج أوامر القسم ث أدناه**

---

## 0) كيف تقرأ هذه الخطة

| رمز | المعنى |
| --- | --- |
| ✅ | منفّذ في الكود حالياً |
| 🔧 | يحتاج تنفيذ/تأكيد على السيرفر أو المحتوى |
| ⛔ | ممنوع / خطر بيانات |
| 📌 | موضع بيانات حساس أو نقطة ترابط |
| 🔁 | يعتمد على خطوة سابقة |

كل قسم يربط: **المصدر → التخزين → العرض → الفهرسة → القياس**.

---

## أ) الهوية والعلامة (Brand)

| الحقل | القيمة المعتمدة | أين تظهر |
| --- | --- | --- |
| الاسم العربي | نور محمد | الهير، About، JSON-LD، meta |
| الاسم الإنجليزي | Nour Mohamed | نفس المواضع |
| البراند | Dev Nour | H1 الهير، عناوين، sitemap |
| الشركة | Mudiri Digi / موديري ديجي | Footer، schema `sameAs`، sitemap، الهير |
| الدور | مؤسس موديري ديجي · Full-Stack · منتجات ومتاجر | Profile.titleAr/En |
| الروابط المقفلة (لا تُحذف من الإعدادات) | `https://mudiridigi.com` · `https://mudiridigi.shop/` | كود المصدر `src/lib/seo.ts` → `MUDIRI` |

**دقة:** روابط Mudiri مقفلة في الكود (footer + JSON-LD + sitemap) وليست اختيارية من الأدمن.

---

## ب) النطاق والبيئات (Environments)

| بيئة | URL | قاعدة البيانات | الرفع |
| --- | --- | --- | --- |
| محلي | `http://localhost:3000` | `file:./dev.db` (نسبي داخل `prisma/`) | `public/uploads` |
| إنتاج Hostinger | `https://dev.mudiridigi.com` | مسار مطلق خارج `hbuilds/current` | `UPLOAD_DIR` مطلق |

### مسارات Hostinger الفعلية (دقيقة)

```text
/home/u194449289/domains/dev.mudiridigi.com/
├── data/                          ← دائم (لا يُمسَح مع النشر)
│   ├── prod.db                    ← SQLite الإنتاج
│   └── uploads/                   ← الصور المرفوعة
├── hbuilds/
│   ├── config/.env                ← متغيرات البيئة (لوحة Hostinger)
│   ├── last-source/               ← مصدر كامل (schema + seed هنا)
│   ├── current/nodejs/            ← تشغيل الإنتاج (قد يخلو من prisma/)
│   └── versions/...               ← نسخ بناء قديمة
```

⛔ لا تضع `prod.db` أو الصور داخل `hbuilds/current` — تُستبدل عند كل Deploy.

---

## ج) البيانات الحساسة (Sensitive Data Map)

### ج-1) أسرار يجب ألا تدخل Git أبداً

| المفتاح | الحساسية | موضع التخزين الصحيح | ملاحظات دقة |
| --- | --- | --- | --- |
| `JWT_SECRET` | 🔴 عالية | `.env` / hPanel فقط | سلسلة طويلة عشوائية؛ تغييرها يسقط جلسات الأدمن |
| `ADMIN_PASSWORD` | 🔴 عالية | `.env` وقت الـ seed فقط | تُهشَّر بـ bcrypt في جدول `User`؛ لا تُخزَّن نصاً صريحاً في DB |
| `ADMIN_EMAIL` | 🟠 متوسطة | `.env` + جدول `User.email` | البريد الظاهر للوجين |
| `DATABASE_URL` | 🔴 عالية | `.env` فقط | مسار ملف SQLite؛ تسريبه = وصول لكل المحتوى والرسائل |
| `UPLOAD_DIR` | 🟠 متوسطة | `.env` فقط | مسار نظام ملفات؛ خطأ المسار = صور مفقودة |

📌 ملف المرجع الآمن (بدون أسرار حقيقية): `.env.example`  
⛔ `.env` و`*.db` و`public/uploads/*` في `.gitignore`.

### ج-2) بيانات شخصية / أعمال (شبه عامة بعد النشر)

| البيان | الجدول/الحقل | العرض العام؟ | تحسس الموضع |
| --- | --- | --- | --- |
| الإيميل | `Profile.email` | نعم (Contact/About) | استخدم بريد عمل رسمي |
| الهاتف / واتساب | `Profile.phone` + روابط Social | نعم | صيغة دولية `2015...` للواتساب |
| الصورة الشخصية | `Profile.avatarUrl` → ملف في uploads | نعم | LCP — أول فيو |
| السيرة | `Profile.bioAr/En` | نعم | AR/EN متطابقان معنىً لا حرفاً |
| السن | `Profile.age` | نعم (الهير إن وُجد) | اختياري؛ لا تختلق رقماً |
| سنوات الخبرة | `Profile.yearsExperience` | نعم | افتراضي 5؛ عدّل من Settings |
| رسائل الزوار | `ContactMessage` | ❌ أدمن فقط | بيانات شخصية للمرسلين |
| تحليلات الزوار | `Analytics*` | ❌ أدمن فقط | مفاتيح زائر/جلسة؛ لا PII كاملة |

### ج-3) ما يظهر للعامة مقابل ما يبقى داخلياً

```text
عام: Profile, Project, Service, Experience, Skill, SocialLink, SiteLink, SiteSetting (جزئياً)
خاص: User.passwordHash, ContactMessage, Analytics*
مختلط: SiteSetting.googleVerification* (meta عامة / ملفات تحقق عامة)
```

---

## د) متغيرات البيئة — قالب إنتاج دقيق

```bash
# 📌 انسخ إلى hbuilds/config/.env أو لوحة Environment في Hostinger

DATABASE_URL="file:/home/u194449289/domains/dev.mudiridigi.com/data/prod.db"
UPLOAD_DIR="/home/u194449289/domains/dev.mudiridigi.com/data/uploads"
NEXT_PUBLIC_SITE_URL="https://dev.mudiridigi.com"

JWT_SECRET="<<32+ chars random — غيّره فوراً>>"
ADMIN_EMAIL="admin@devnour.com"
ADMIN_PASSWORD="<<كلمة مرور قوية — تُستخدم عند seed فقط>>"
```

### ترابط حرج

| متغير | إن أخطأ ماذا يحدث؟ |
| --- | --- |
| `DATABASE_URL` نسبي `file:./prod.db` | لوجين فاشل / جداول فارغة / بيانات seed في ملف آخر |
| `NEXT_PUBLIC_SITE_URL=localhost` | canonical وOG وsitemap بروابط خاطئة → SEO ينهار |
| بدون `UPLOAD_DIR` | الصور تُرفع داخل `current` وتختفي بعد Redeploy |
| `JWT_SECRET` ضعيف/افتراضي | خطر اختراق جلسة الأدمن |

---

## هـ) نموذج البيانات والترابط (Schema Graph)

```text
User ──────────────────── أدمن واحد (أو أكثر) — JWT
Profile ───────────────── هوية الموقع (1 صف تقريباً)
SiteSetting ───────────── SEO افتراضي + تحقق Google + accent
Project ─┬─ slug ──────── /[locale]/projects/[slug]
Service ─┴─ slug ──────── /[locale]/services/[slug]
Experience / Skill ────── صفحات قائمة + الهير (skills chips)
SocialLink / SiteLink ─── /links + الهير/الفوتر
ContactMessage ←──────── POST /api/contact
AnalyticsVisitor ─┬─ AnalyticsSession ─┬─ AnalyticsPageView
                  │                    └─ AnalyticsEvent
                  └────────────────────── (عبر visitorId)
```

**ملفات المصدر:** `prisma/schema.prisma` · العميل: `@prisma/client` · الوصول: `src/lib/db.ts` · قراءة آمنة: `src/lib/content.ts`

---

## و) خريطة الصفحات العامة (Public IA)

| المسار | الغرض | CTA أساسي | SEO |
| --- | --- | --- | --- |
| `/[locale]` | هير + أعمال + خبرة + خدمات + مهارات | استشارة / أعمال / خدمات | `buildPageMetadata` + صورة |
| `/[locale]/projects` | قائمة أعمال | ConsultCTA | keywords أعمال |
| `/[locale]/projects/[slug]` | دراسة حالة | تواصل | meta من DB |
| `/[locale]/services` | خدمات ومجال العمل | ConsultCTA | keywords خدمات |
| `/[locale]/services/[slug]` | تفصيل خدمة + أسعار | تواصل | meta من DB |
| `/[locale]/experience` | مسار مهني | — | عنوان+وصف |
| `/[locale]/skills` | مهارات مجمّعة | — | عنوان+وصف |
| `/[locale]/about` | من أنا + روابط Mudiri | استشارة | profile keywords |
| `/[locale]/contact` | نموذج + واتساب | إرسال رسالة | free consult keywords |
| `/[locale]/links` | لينك-إن-بايو | استشارة | links keywords |

**اللغات:** `ar` · `en` · كشف تلقائي · hreflang + `x-default→en`

---

## ز) لوحة الأدمن (Admin Map)

| المسار | يدير | يلمس في DB |
| --- | --- | --- |
| `/admin/login` | دخول | `User` + JWT cookie |
| `/admin` | لوحة | إحصاءات سريعة |
| `/admin/projects` | مشاريع | `Project` + autoSeo |
| `/admin/services` | خدمات | `Service` + autoSeo |
| `/admin/experience` | خبرات | `Experience` |
| `/admin/skills` | مهارات | `Skill` |
| `/admin/social` | سوشيال | `SocialLink` |
| `/admin/links` | روابط | `SiteLink` |
| `/admin/messages` | رسائل | `ContactMessage` |
| `/admin/analytics` | زيارات | `Analytics*` |
| `/admin/settings` | بروفايل + SEO + Google + صورة | `Profile` + `SiteSetting` |

**حماية:** middleware + JWT httpOnly · rate limit على login/contact/upload · Zod على الـ APIs.

---

## ح) الوسائط والصور (Media Pipeline)

```text
Admin ImageUploader
    → POST /api/admin/upload
        → يكتب إلى UPLOAD_DIR (أو public/uploads محلياً)
        → يُرجع URL: /api/media/<filename>
    → يُحفظ المسار في Profile.avatarUrl / Project.coverUrl / SiteSetting.ogImageUrl

الزائر يطلب /uploads/x أو /api/media/x
    → rewrite uploads → api/media
    → resolveUploadPath يبحث: UPLOAD_DIR ثم public/uploads
    → ProfilePhoto: unoptimized للملفات المحلية (تفادي فشل _next/image)
```

### قائمة تحقق إصلاح الصورة المكسورة

1. 🔧 تأكد `UPLOAD_DIR` مطلق وموجود (`mkdir -p .../data/uploads`)
2. 🔧 أعد رفع الصورة من Settings بعد ضبط المتغير
3. 🔧 تأكد أن `avatarUrl` في DB يشير لملف موجود
4. 🔧 `NEXT_PUBLIC_SITE_URL` = الدومين الحي (للـ OG)

---

## ط) الهوية البصرية (Design System)

| توكن | داكن | فاتح | ممنوع (حسب طلبك) |
| --- | --- | --- | --- |
| `--accent` | `#ff2d55` كورال | `#e11d48` | بني / أخضر / بنفسجي / أزرق كسمة رئيسية |
| `--ink` | `#ffb800` عنبر | `#d97706` | — |
| `--bg` | `#0a0a0b` | `#f7f7f8` (ليس كريمي دافئ) | كريم `#F4F1EA` كخلفية افتراضية |
| خطوط | Syne (عرض) + Geist + IBM Plex Arabic | — | Inter/Roboto كعرض رئيسي |

**ملفات:** `src/app/globals.css` · `src/app/layout.tsx` (متغيرات الخطوط `--font-body/display/arabic`)

**الهير (مكوّن أول فيو):** براند → صورة مربعة → اسم → لقب → إحصاءات (سن/خبرة/مهارات/أعمال) → chips مهارات → bio → Mudiri → CTAs.

---

## ي) SEO · GEO · AEO · فهرسة

### ي-1) أصول تقنية ✅

| الأصل | المسار | الوظيفة |
| --- | --- | --- |
| Sitemap | `/sitemap.xml` ← `src/app/sitemap.ts` | صفحات + مشاريع + خدمات + Mudiri URLs |
| Robots | `/robots.txt` | يسمح بمحركات وبوتات AI؛ يمنع `/admin` و`/api/admin` |
| llms.txt | `/llms.txt` · `/llms-full.txt` | اكتشاف وكلاء الذكاء |
| JSON-LD | `JsonLd` في layout | Person + WebSite + Organization + CTA consult |
| Favicon/PWA | `/icon` · `/apple-icon` · `/manifest.webmanifest` | هوية تبويب المتصفح |
| Meta موحّد | `buildPageMetadata()` في `src/lib/seo.ts` | title/desc/keywords/OG/Twitter/hreflang |
| autoSeo | عند حفظ مشروع/خدمة | slug + meta + keywords تلقائياً |

### ي-2) كلمات مفتاحية — طبقات

1. **طبقة موقع:** نور محمد، Dev Nour، موديري ديجي، Laravel، Flutter، Next.js، SEO، GEO، AEO  
2. **طبقة صفحة:** لكل route في `generateMetadata`  
3. **طبقة كيان:** `keywordsAr/En` على Project/Service من الأدمن  

### ي-3) قائمة تحقق Google Search Console 🔧

- [ ] `NEXT_PUBLIC_SITE_URL=https://dev.mudiridigi.com`
- [ ] رفع/لصق `googleVerificationMeta` أو ملف `googleXXXX.html` من Settings
- [ ] إرسال `https://dev.mudiridigi.com/sitemap.xml`
- [ ] فحص URL للهوم `/ar` و`/en`
- [ ] مراقبة Coverage / Experience (CWV)

### ي-4) أدوات GitHub الموصى بها (تدقيق مستمر)

| أداة | الرابط | دورها في خطتك |
| --- | --- | --- |
| agentic-seo | https://github.com/addyosmani/agentic-seo | تدقيق AEO / llms / robots |
| next-ai-ready | https://github.com/mustcanbedo/next-ai-ready | طبقة Agent للـ Next |
| seo-mesh | https://github.com/mehmetcanozturk/seo-mesh | JSON-LD مكوّني |
| PageSpeed Insights | Google | هدف 100 Desktop/Mobile |
| GSC + Bing Webmaster | Google/Microsoft | فهرسة رسمية |

---

## ك) الأداء وPageSpeed (هدف 100)

| محرّك النتيجة | إجراء منفّذ / مطلوب |
| --- | --- |
| LCP | صورة الهير `priority` + `unoptimized` محلي؛ لا blur على الانتقال |
| CLS | أبعاد ثابتة للصورة (مربع width=height) |
| INP/TBT | analytics heartbeat 20s بدل 5s؛ PageTransition أخف |
| الخطوط | `display:swap` + أوزان أقل + ربط متغيرات CSS الصحيح |
| الطرف الثالث | لا GA ثقيل — تحليلات first-party فقط |

🔧 بعد كل Deploy: أعد قياس PSI Desktop + Mobile وسجّل الرقم هنا.

| تاريخ | Desktop Perf | Mobile Perf | A11y | BP | SEO |
| --- | --- | --- | --- | --- | --- |
| قبل التحسين | 53 | 94 | 96 | 92 | 2/3 |
| بعد النشر (املأ) | — | — | — | — | — |

---

## ل) التحليلات وتتبع الزوار

```text
AnalyticsProvider (عميل)
  → visitorKey / sessionKey في localStorage/sessionStorage
  → أحداث: page_enter, page_leave, navigate, click, heartbeat
  → POST /api/analytics/collect
  → جداول Analytics*
  → لوحة /admin/analytics
```

**دقة:** كل حدث له `clientEventId` فريد لتقليل التكرار.  
**خصوصية:** لا ترسل أسماء/إيميلات الزوار؛ راقب المسارات والـ CTA فقط (`data-track`).

CTAs المطلوب تتبعها في مجالك:

- `hero-consult` · `hero-work` · `hero-services`
- روابط Mudiri / Shop
- ConsultCTA في صفحات القائمة
- إرسال نموذج التواصل

---

## م) الأمان (Security Checklist)

- [x] JWT httpOnly للكوكي
- [x] حماية `/admin` و`/api/admin`
- [x] Rate limit: login / contact / upload
- [x] Zod على المدخلات
- [x] أنواع صور مسموحة + حد حجم
- [ ] 🔧 تدوير `JWT_SECRET` و`ADMIN_PASSWORD` بعد الإطلاق
- [ ] 🔧 HTTPS إجباري على الدومين
- [ ] 🔧 نسخ احتياطي دوري لـ `prod.db` و`uploads/`
- [ ] ⛔ عدم تشغيل `seed-expand` على إنتاج فيه محتوى حقيقي إلا بعد backup (يمسح skills/services/…)

---

## ن) محتوى البذرة والبذر الآمن

| أمر | ماذا يفعل | خطر على الإنتاج |
| --- | --- | --- |
| `npx prisma db push` | يزامن الجداول | منخفض (لا يمسح صفوفاً عادة) |
| `npx tsx prisma/seed.ts` | أدمن + أساس | يحدّث passwordHash للإيميل المعطى |
| `npx tsx prisma/seed-expand.ts` | محتوى موسّع | 🔴 `deleteMany` على skills/services/… |

**تشغيل أول مرة فقط (من `hbuilds/last-source`):**

```bash
export DATABASE_URL="file:/home/u194449289/domains/dev.mudiridigi.com/data/prod.db"
export ADMIN_EMAIL="..."
export ADMIN_PASSWORD="..."
npm ci
npx prisma db push
npx tsx prisma/seed.ts
npx tsx prisma/seed-expand.ts
```

**بعد الإطلاق:** عدّل المحتوى من الأدمن فقط.

---

## س) مهارات مضافة من منظومة GitHub/SEO (فئة Indexing & AI)

تُزرع عبر `seed-expand` أو تُضاف يدوياً من Admin:

- Agentic SEO (AEO)
- llms.txt · AI crawlers
- JSON-LD / Schema.org
- Core Web Vitals
- next-intl i18n
- Prisma + SQLite
- First-party analytics
- Lighthouse / CWV tuning

فئات العرض في الهوم: Languages · Frameworks · Platforms · Ads · **Indexing**

---

## ع) نشر Hostinger — تسلسل من الألف للياء

```text
1) إعداد مجلد دائم
   mkdir -p ~/domains/dev.mudiridigi.com/data/uploads

2) ضبط env في اللوحة (القسم د) — مطلقة لا نسبية

3) ربط GitHub main / Pull أحدث commit

4) Build: npm ci && npm run build
   (يبني ويشغّل prisma generate + db push)

5) إن كانت الجداول فارغة أول مرة فقط:
   من last-source مع DATABASE_URL المُصدَّر → seed

6) Restart التطبيق

7) دخّل /admin → Settings:
   - ارفع الصورة
   - عيّن age + yearsExperience
   - siteUrl = https://dev.mudiridigi.com
   - تحقق Google

8) اختبر:
   /ar · /en · الصورة · اللوجين · /sitemap.xml · /llms.txt · /icon

9) GSC + PSI → سجّل النتائج في جدول القسم ك
```

---

## ف) مصفوفة الترابط الشاملة (Dependency Matrix)

| إن تغيّر… | يجب مراجعة… |
| --- | --- |
| `Profile.avatarUrl` | الهير، About، Contact، Links، JSON-LD image، OG إن استُخدم كبديل |
| `SiteSetting.siteUrl` / `NEXT_PUBLIC_SITE_URL` | sitemap، canonical، OG، llms، schema |
| `Project.slug` | روابط داخلية، sitemap، صفحة التفاصيل |
| `MUDIRI` في الكود | footer، schema، sitemap، الهير |
| `UPLOAD_DIR` | كل الصور القديمة (انسخ الملفات للمسار الجديد) |
| `JWT_SECRET` | إعادة تسجيل دخول الأدمن |
| لغة الرسائل `messages/*.json` | نصوص الهير والـ CTA والـ a11y labels |

---

## ص) معايير قبول نهائية (Definition of Done)

الموقع يُعتبر «جاهز إطلاق» فقط إذا:

1. ✅ الهير يعرض صورة مربعة حقيقية بجانب الاسم والبيانات  
2. ✅ اللوجين يعمل على نفس `DATABASE_URL` المطلق  
3. ✅ الصور تبقى بعد Redeploy (`UPLOAD_DIR`)  
4. ✅ `NEXT_PUBLIC_SITE_URL` هو الدومين الحي  
5. ✅ `/sitemap.xml` و`/robots.txt` و`/llms.txt` و`/icon` تعمل  
6. ✅ CTA استشارة واضحة في الهير + الهيدر + صفحات الخدمات/الأعمال  
7. ✅ Mudiri Digi ظاهرة وقابلة للزحف (footer + schema)  
8. 🔧 PSI Desktop ≥ 90 (هدف 100) وMobile ≥ 95  
9. 🔧 GSC: الموقع مُتحقق + sitemap مُرسل  
10. 🔧 لا أسرار في Git · backup لـ db/uploads موجود  

---

## ق) جدول تشغيل أسبوعي (بعد الإطلاق)

| اليوم | مهمة |
| --- | --- |
| يوم 1 | مراجعة Analytics: أكثر الصفحات / نقرات CTA |
| يوم 2 | رد رسائل Contact + تحديث حالة read |
| يوم 3 | تحسين meta لمشروع/خدمة ضعيفة الظهور |
| يوم 4 | قياس PSI وتسجيل الرقم |
| يوم 5 | مراجعة محتوى سوشيال/روابط من /links |
| يوم 6 | Backup: نسخ `prod.db` + `uploads/` |
| يوم 7 | مراجعة GSC (استعلامات، أخطاء تغطية) |

---

## ر) سجل قرارات تصميم/تقنية (للدقة التاريخية)

| قرار | السبب |
| --- | --- |
| SQLite بدل Postgres | استضافة Hostinger بسيطة؛ ملف واحد للنسخ |
| مسار DB/Uploads مطلق خارج current | Hostinger يمسح `hbuilds/current` |
| كورال + عنبر | تمييز بصري بعيد عن البني/الأخضر/البنفسجي/الأزرق |
| First-party analytics | خصوصية + أداء بدون سكربتات ثقيلة |
| next-intl | موقع ثنائي اللغة مع اكتشاف المتصفح |
| Mudiri مقفلة في الكود | عقد attribution إلزامي |

---

## ش) ملفات مرجعية سريعة في المستودع

| الملف | الدور |
| --- | --- |
| `خطة-التطوير-الشاملة.md` | هذا المستند |
| `.env.example` | قالب env بدون أسرار |
| `README.md` | تشغيل سريع + Hostinger |
| `prisma/schema.prisma` | مصدر الحقيقة للجداول |
| `src/lib/seo.ts` | MUDIRI + autoSeo + buildPageMetadata |
| `src/lib/uploads.ts` | مسارات الرفع |
| `src/lib/content.ts` | قراءة آمنة للمحتوى |
| `messages/ar.json` · `en.json` | النصوص الظاهرة |
| `AGENTS.md` | قواعد Next + AEO للوكيل |
| `prisma/seed-skills.ts` | زرع مهارات آمن بدون مسح |
| `scripts/backup.mjs` | نسخ احتياطي DB + uploads |
| `scripts/post-deploy-check.mjs` | قائمة تحقق ما بعد النشر |

---

## ت) خطواتك الفورية الآن (Ordered)

### ✅ تم في المستودع (نفّذها الوكيل)

- [x] خطة شاملة في `خطة-التطوير-الشاملة.md`
- [x] رفع دائم `UPLOAD_DIR` + `/api/media`
- [x] هير مربّع + إحصاءات + CTAs
- [x] ألوان كورال/عنبر
- [x] favicon / manifest / keywords / Twitter / JSON-LD
- [x] ConsultCTA على experience/skills/تفاصيل المشاريع والخدمات
- [x] `npm run db:seed-skills` (upsert آمن)
- [x] `npm run db:backup` + `npm run postdeploy:check`
- [x] قواعد AEO في `AGENTS.md`

### 🔧 نفّذ على Hostinger الآن (نسخ/لصق)

```bash
# 1) مجلد دائم
mkdir -p ~/domains/dev.mudiridigi.com/data/uploads

# 2) في لوحة env ثبّت القيم المطلقة من القسم د

# 3) بعد Pull + Rebuild، من last-source:
cd ~/domains/dev.mudiridigi.com/hbuilds/last-source
export DATABASE_URL="file:/home/u194449289/domains/dev.mudiridigi.com/data/prod.db"
export UPLOAD_DIR="/home/u194449289/domains/dev.mudiridigi.com/data/uploads"
export NEXT_PUBLIC_SITE_URL="https://dev.mudiridigi.com"
npm ci
npx prisma db push
npx tsx prisma/seed-skills.ts
node scripts/backup.mjs

# 4) Restart من hPanel
# 5) /admin → Settings: صورة + سن + خبرة + siteUrl الحي + Google
# 6) تحقق: /ar /sitemap.xml /llms.txt /icon
# 7) GSC أرسل sitemap · PSI سجّل في جدول القسم ك
# 8) غيّر JWT_SECRET وكلمة الأدمن إن كانت افتراضية
```

---

## ث) ملاحظة حوكمة المستند

- حدّث هذا الملف عند كل تغيير جوهري في المسارات أو env أو نموذج البيانات.  
- لا تضع فيه كلمات مرور حقيقية أو JWT حقيقي.  
- عند تعارض بين هذا الملف والكود: **الكود + schema هما مصدر الحقيقة**، ثم تُحدَّث الخطة.

---

*نهاية خطة الألف إلى الياء — Dev Nour × Mudiri Digi*
